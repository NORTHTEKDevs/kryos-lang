[package]
name = "kryos-toml-pkg"
version = "0.1.0"
edition = "2026"
description = "TOML subset parser (tables, scalars) with typed getters, pure Kryos."
authors = ["NORTHTEK <info@northtek.io>"]
license = "Apache-2.0"

[dependencies]
