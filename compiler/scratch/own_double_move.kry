// Test double move detection
struct Item {
    name: str
}

fn consume(i: Item) {
    println("consumed: " + i.name)
}

fn main() {
    let item = Item { name: "test" }
    consume(item)
    consume(item)
}
