// Test 03: Array of structs
struct Point {
    x: i64,
    y: i64,
}

fn main() {
    let points = [
        Point { x: 1, y: 2 },
        Point { x: 3, y: 4 },
        Point { x: 5, y: 6 },
    ]
    for p in points {
        println(to_string(p.x) + "," + to_string(p.y))
    }
}
