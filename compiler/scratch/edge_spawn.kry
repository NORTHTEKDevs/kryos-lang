fn main() {
    let ch = chan()
    
    spawn {
        send(ch, 10)
    }
    spawn {
        send(ch, 20)
    }
    spawn {
        send(ch, 30)
    }
    
    let mut total = 0
    for i in range(0, 3) {
        total = total + recv(ch)
    }
    println(to_string(total))
}
