// Test 63: Recursive struct (linked list) -- should this work?
struct Node {
    value: i64,
    next: Node,
}

fn main() {
    // This should probably be a compile error (infinite size struct)
    let n = Node { value: 1, next: Node { value: 2, next: Node { value: 3 } } }
    println(n.value)
}
