// Test 87: Match on enum without covering all variants and no default
enum Direction {
    North,
    South,
    East,
    West,
}

fn main() {
    let d = Direction.West
    let name = match d {
        North => "north",
        South => "south",
    }
    println(name)
}
