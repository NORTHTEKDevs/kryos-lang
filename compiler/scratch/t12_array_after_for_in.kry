// Test 12: for-in loop over array, then using the array after
fn main() {
    let arr = [10, 20, 30, 40, 50]

    let mut sum = 0
    for item in arr {
        sum = sum + item
    }
    println(sum)

    // Use array after for-in loop
    println(len(arr))
    println(arr[2])
}
