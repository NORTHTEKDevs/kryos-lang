// Test 05: Recursive function with wrong return type
// This should be a type error: returns str but declared i64
fn bad_fib(n: i64) -> i64 {
    if n <= 1 {
        return "not a number"
    }
    return bad_fib(n - 1) + bad_fib(n - 2)
}

fn main() {
    println(bad_fib(5))
}
