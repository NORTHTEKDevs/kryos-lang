// Test 30: Channel with send but no recv (should not hang forever?)
fn main() {
    let ch = chan()

    spawn {
        send(ch, 99)
    }

    // Deliberately NOT receiving from ch
    println("done")
}
