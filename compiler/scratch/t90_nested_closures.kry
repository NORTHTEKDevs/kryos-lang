// Test 90: Nested closures
fn main() {
    let make_adder = fn(x) {
        return fn(y) { return x + y }
    }
    let add5 = make_adder(5)
    let add10 = make_adder(10)
    println(add5(3))   // should be 8
    println(add10(3))  // should be 13
}
