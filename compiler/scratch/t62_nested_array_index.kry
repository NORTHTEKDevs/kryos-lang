// Test 62: Nested array indexing with computed indices
fn main() {
    let arr = [100, 200, 300, 400, 500]
    let indices = [4, 2, 0, 3, 1]

    // Index with another array's value
    let mut i = 0
    while i < 5 {
        println(arr[indices[i]])
        i = i + 1
    }
}
