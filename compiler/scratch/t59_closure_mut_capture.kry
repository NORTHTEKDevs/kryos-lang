// Test 59: Closure capturing mutable variable
fn main() {
    let mut counter = 0
    let increment = fn() {
        counter = counter + 1
        return counter
    }
    println(increment())  // should be 1
    println(increment())  // should be 2
    println(increment())  // should be 3
}
