fn main() {
    let name = "World"
    let x = 42
    let msg = "Hello " + name + "! x=" + to_string(x)
    println(msg)
    
    // Test string escape sequences
    let escaped = "tab:\there\nnewline"
    println(escaped)
}
