// Test that @copy struct is truly independent after copy
@copy
struct Vec2 {
    x: f64,
    y: f64
}

fn main() {
    let mut a = Vec2 { x: 1.0, y: 2.0 }
    let mut b = a
    b.x = 99.0
    println("a.x: " + to_string(a.x))
    println("b.x: " + to_string(b.x))
    // a.x should still be 1.0
}
