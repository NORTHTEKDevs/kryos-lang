// Test mutating struct fields
struct Point {
    x: f64,
    y: f64
}

fn main() {
    let mut p = Point { x: 1.0, y: 2.0 }
    println("before: " + to_string(p.x) + ", " + to_string(p.y))
    p.x = 10.0
    p.y = 20.0
    println("after: " + to_string(p.x) + ", " + to_string(p.y))
}
