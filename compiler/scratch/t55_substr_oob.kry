// Test 55: substr with out of bounds indices
fn main() {
    let s = "hello"
    // End beyond length
    let r1 = substr(s, 0, 100)
    println(r1)

    // Start beyond length
    let r2 = substr(s, 10, 20)
    println(r2)

    // Negative start
    let r3 = substr(s, -1, 3)
    println(r3)
}
