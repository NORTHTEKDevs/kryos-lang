// Test pop on empty array
fn main() {
    let mut arr: [i64] = []
    push(arr, 42)
    let val = pop(arr)
    println("popped: " + to_string(val))
    println("len after pop: " + to_string(len(arr)))
    // Now pop from empty
    let val2 = pop(arr)
    println("pop empty: " + to_string(val2))
}
