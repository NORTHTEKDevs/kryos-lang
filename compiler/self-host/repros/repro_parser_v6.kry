// Test: just a brace inside a string literal — does it confuse the lexer?
fn first() { println("first") }

fn problematic() {
    let mut text = ""
    while text != "{" {
        text = "y"
    }
}

fn last() { println("last") }
