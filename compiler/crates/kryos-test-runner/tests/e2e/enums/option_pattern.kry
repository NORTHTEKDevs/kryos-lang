// Test: Option-like enum (type-checks + compiles on both backends)
enum Option {
    Some(i32),
    None,
}

fn unwrap_or(opt: Option, default: i32) -> i32 {
    match opt {
        Option::Some(val) => val,
        Option::None => default,
    }
}
