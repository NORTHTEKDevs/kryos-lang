// Test: struct with impl methods (type-checks + compiles on both backends)
struct Counter {
    value: i32,
}

impl Counter {
    fn new() -> Counter {
        return Counter { value: 0 }
    }

    fn increment(self) -> i32 {
        return self.value + 1
    }
}
