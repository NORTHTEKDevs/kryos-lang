// run-expect: 1
@capabilities(process)
fn main() {
    // PATH is set on virtually every OS. If env_get returns a non-empty string,
    // its length will be > 0.
    let path = env_get("PATH")
    if len(path) > 0 {
        println("1")
    } else {
        println("0")
    }
}
