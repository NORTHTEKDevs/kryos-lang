// Test: lambda (type-checks + compiles on both backends)
fn apply(f: fn(i32) -> i32, x: i32) -> i32 {
    return f(x)
}

fn main() {
    let double = fn(x: i32) -> i32 { return x * 2 }
    let result = apply(double, 21)
}
