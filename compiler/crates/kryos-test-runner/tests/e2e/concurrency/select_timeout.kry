// run-expect: timeout fired
fn main() {
    let ch = chan()

    select {
        recv ch => {
            println("received")
        }
        timeout 100 => {
            println("timeout fired")
        }
    }
}
