// run-expect: timeout fired
// See note in select_basic.kry — branch syntax is `<binding> <channel> =>`.
fn main() {
    let ch = chan()

    select {
        v ch => {
            println("received")
        }
        timeout 100 => {
            println("timeout fired")
        }
    }
}
