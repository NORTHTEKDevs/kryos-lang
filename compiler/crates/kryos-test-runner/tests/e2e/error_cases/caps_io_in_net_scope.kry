// expect-error: fs:write
// A @capabilities(net) function cannot use file_write (which requires fs:write).

@capabilities(net)
fn save_response(body: str) {
    file_write("response.txt", body)
}

fn main() {
    save_response("hello")
}
