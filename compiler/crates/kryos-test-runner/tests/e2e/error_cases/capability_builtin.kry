// expect-error: builtin `file_write` requires `fs:write` capability
// A @capabilities(net) function cannot call file_write (needs fs:write).

@capabilities(net)
fn bad_save() {
    file_write("out.txt", "data")
}

fn main() {
    bad_save()
}
