// Validates regex builtins link and execute through LLVM --release.
// expect-stdout: true
// expect-stdout: false
fn main() {
    let re = regex_new("^[a-z]+$")
    println(to_string(regex_match(re, "hello")))
    println(to_string(regex_match(re, "Hello")))
}
