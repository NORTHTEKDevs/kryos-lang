[package]
name = "mcp-server"
version = "0.1.0"
description = "MCP JSON-RPC 2.0 server over stdio, scaffolded by kryos-init-templates."
license = "Apache-2.0"

[capabilities]
allowed = ["compute", "io"]
